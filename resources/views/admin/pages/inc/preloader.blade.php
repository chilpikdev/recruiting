<div class="preloader flex-column justify-content-center align-items-center">
    <img class="animation__wobble" src="{{ asset(config('settings.site_logo')) }}" alt="{{ asset(config('settings.site_name')) }}" height="60" width="60">
</div>
